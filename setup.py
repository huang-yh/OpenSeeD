from setuptools import setup
from setuptools import find_packages

# for install, do: pip install -ve .

setup(
    name='openseed', 
    packages=['openseed'],
    package_dir={'openseed': 'openseed'})
